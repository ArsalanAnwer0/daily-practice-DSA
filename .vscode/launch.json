{
  "version": "0.2.0",
  "configurations": [
     
    {
      "name": "C++ Launch",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "stopAtEntry": false,  // You can set this to true if you want to stop at the first line of code for debugging
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,  // Use the integrated terminal
      "MIMode": "lldb",
      "preLaunchTask": "C/C++: clang++ build active file"
    }
  ]
}
